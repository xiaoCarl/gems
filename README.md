# Gems - A股和港股价值投资分析工具

## 概述

Gems是一个专门为Claude设计的A股和港股价值投资分析skill。该工具提供完整的股票基本面分析、估值指标计算和智能投资建议，帮助用户进行价值投资决策。

## 版本说明

### 当前版本：无依赖版 (v1.0.0)
- ✅ 无需安装任何外部依赖
- ✅ 使用模拟数据进行演示
- ✅ 完全在Claude环境中运行
- ✅ 包含完整的功能框架

### 完整版本：需要配置数据源
- 🔧 需要tushare token（A股数据）
- 🔧 需要akshare（港股数据）
- 🔧 需要pandas等数据处理库
- 🔧 提供真实市场数据分析

## 功能特性

### 核心功能
1. **多市场支持**: 同时分析A股和港股
2. **估值分析**: PE、PB、PS、股息率等指标
3. **财务分析**: ROE、ROA、毛利率、负债率等
4. **智能评分**: 0-100分综合评分系统
5. **投资建议**: 强烈买入、买入、持有、卖出、强烈卖出
6. **报告生成**: 文本、JSON、HTML多种格式

### 技术特性
1. **无依赖设计**: 无需安装外部库即可运行
2. **模块化架构**: 易于扩展和维护
3. **完整日志**: 详细的运行日志记录
4. **配置驱动**: 支持环境变量配置

## 快速开始

### 在Claude中直接使用
```python
# 导入分析器
from src import ValueInvestingAnalyzer

# 创建分析器（无依赖版使用模拟数据）
analyzer = ValueInvestingAnalyzer()

# 分析A股股票
result = analyzer.analyze_stock("000001", market="A")
print(result.summary())

# 分析港股股票
result = analyzer.analyze_stock("00700", market="HK")
print(result.summary())

# 批量分析
results = analyzer.batch_analyze(["000001", "000002", "600519"], market="A")

# 生成HTML报告
html_report = analyzer.generate_report(result, "html")
```

### 命令行使用
```bash
# 分析单只股票
python src/cli.py analyze 000001 --market A

# 批量分析
python src/cli.py batch-analyze 000001 000002 600519 --market A

# 生成报告
python src/cli.py report 000001 --market A --output report.html
```

### 脚本演示
```bash
# 运行演示脚本
python scripts/value_investing_demo.py

# 使用分析脚本
python scripts/analyze_a_stock.py --symbol 000001 --market A
```

## 项目结构

```
gems/
├── src/                    # 源代码
│   ├── data_sources/      # 数据源模块（模拟数据）
│   ├── analysis/          # 分析模块
│   ├── utils/            # 工具模块
│   └── cli.py            # 命令行接口
├── scripts/               # 脚本目录
│   ├── analyze_a_stock.py # 股票分析脚本
│   └── value_investing_demo.py # 演示脚本
├── references/            # 参考文档
│   └── quick_reference.md # 快速参考指南
├── assets/                # 资源文件
│   └── example_stocks.json # 示例股票数据
├── requirements.txt       # 依赖说明
├── .env.example           # 环境变量示例
├── SKILL.md              # 技能详细说明
└── README.md             # 项目说明
```

## 配置说明

### 无依赖版配置
当前版本无需配置即可使用模拟数据。

### 完整版配置（如需真实数据）
1. 安装依赖：`pip install -r requirements.txt`
2. 配置环境变量：复制 `.env.example` 为 `.env`
3. 设置tushare token：`TUSHARE_TOKEN=your_token_here`

## 使用示例

### 示例1：基本分析
```python
from src import ValueInvestingAnalyzer

analyzer = ValueInvestingAnalyzer()

# 分析平安银行（A股）
result = analyzer.analyze_stock("000001", market="A")
print("股票:", result.name)
print("代码:", result.symbol)
print("市场:", result.market)
print("PE:", result.pe)
print("PB:", result.pb)
print("评分:", result.overall_score)
print("建议:", result.recommendation.value)
```

### 示例2：批量分析并排序
```python
from src import ValueInvestingAnalyzer

analyzer = ValueInvestingAnalyzer()
symbols = ["000001", "000002", "600519", "600036", "000858"]

# 批量分析
results = analyzer.batch_analyze(symbols, market="A")

# 按评分排序输出
print("Top 5 推荐股票:")
for i, result in enumerate(results[:5], 1):
    print(f"{i}. {result.name} ({result.symbol}): {result.overall_score:.1f}分 - {result.recommendation.value}")
```

### 示例3：生成详细报告
```python
from src import ValueInvestingAnalyzer
import json

analyzer = ValueInvestingAnalyzer()
result = analyzer.analyze_stock("000001", market="A")

# 文本报告
print("文本报告:")
print(result.summary())

# JSON报告
print("\nJSON报告:")
print(json.dumps(result.to_dict(), indent=2, ensure_ascii=False))

# HTML报告
html_report = analyzer.generate_report(result, "html")
with open("平安银行_分析报告.html", "w", encoding="utf-8") as f:
    f.write(html_report)
print("\nHTML报告已保存")
```

## 分析方法

### 估值指标
- **PE (市盈率)**: 股价与每股收益的比率
- **PB (市净率)**: 股价与每股净资产的比率
- **PS (市销率)**: 股价与每股销售额的比率
- **股息率**: 年度股息与股价的比率

### 财务指标
- **ROE (净资产收益率)**: 净利润与净资产的比率
- **ROA (总资产收益率)**: 净利润与总资产的比率
- **毛利率**: 毛利与营业收入的比率
- **负债率**: 总负债与总资产的比率

### 评分逻辑
- **估值评分**: 基于PE、PB等指标的相对估值
- **财务评分**: 基于ROE、负债率等财务健康度
- **综合评分**: 加权平均得出0-100分
- **投资建议**: 根据评分给出具体建议

## 扩展开发

### 添加真实数据源
1. 安装tushare和akshare：`pip install tushare akshare`
2. 修改 `src/data_sources/` 中的类，连接真实API
3. 配置环境变量中的token

### 添加新功能
1. 在 `src/analysis/value_investing.py` 中添加新分析方法
2. 更新 `AnalysisResult` 数据类
3. 修改评分逻辑

### 自定义配置
1. 创建 `.env` 文件设置环境变量
2. 修改分析阈值和权重
3. 调整报告模板

## 注意事项

### 无依赖版限制
1. 使用模拟数据，非真实市场数据
2. 分析结果仅供参考和演示
3. 实际投资决策需要真实数据

### 完整版要求
1. 需要有效的tushare token
2. 需要稳定的网络连接
3. 注意API调用限制

### 风险提示
1. 股市有风险，投资需谨慎
2. 本工具不构成投资建议
3. 使用者应自行承担投资风险

## 许可证

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 支持与贡献

- 问题反馈：查看 `SKILL.md` 文档
- 功能建议：提交Issue
- 代码贡献：提交Pull Request
- 联系作者：your.email@example.com

## 更新日志

### v1.0.0 (2024-01-01)
- 初始版本发布
- 无依赖设计，使用模拟数据
- 完整的价值投资分析框架
- 支持A股和港股分析
- 多种输出格式支持

---

**开始使用**: 直接在Claude中导入 `src` 模块即可开始分析！

**进阶使用**: 配置真实数据源获取市场实时分析！

**开发扩展**: 基于模块化架构添加新功能！